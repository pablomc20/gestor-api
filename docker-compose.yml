version: '3.8'

services:
  gestor-api:
    build: .
    container_name: gestor-api
    ports:
      - "8082:8082"
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SERVER_PORT_BACK=8082
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped